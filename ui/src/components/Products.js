import React from "react";

const Products = props => (
	<div>
		<p>
			<strong>Accounts Book - Products</strong>
		</p>
		<hr style={{ borderTop: "1px dashed #8c8b8b" }}/>
		<p>This is a Products page, user can see after logged in.</p>
	</div>
);

export default Products;