import React from "react";

const Home = props => (
	<div>
		<p>
			<strong>Accounts Book - Home</strong>
		</p>
		<hr style={{ borderTop: "1px dashed #8c8b8b" }}/>
		<p> This is a home page, user can see without login. </p>
	</div>
);

export default Home;